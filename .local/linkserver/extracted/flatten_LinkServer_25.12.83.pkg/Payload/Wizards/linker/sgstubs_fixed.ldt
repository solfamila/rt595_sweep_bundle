 <#if (tzProjType?? & tzProjType == "secure") >
 <#if (SG_LOCATION?? & SG_LOCATION == "fixed")>
    /* Place TrustZone Secure Gateway veneers in 'SG_veneer_table' memory */
    .gnu.sgstubs : ALIGN (32)
    {
        . = ALIGN(32);
        _start_sg = .;
        *(.gnu.sgstubs*)
        . = ALIGN(32);
        _end_sg = .;
    } > SG_veneer_table
</#if>
</#if>